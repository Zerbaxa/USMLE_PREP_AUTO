---
name: usmle
description: UWorld 문제 해설(스크린샷/텍스트)을 받아 내 자료와 Anki를 전부 검색해서 출처 표기된 주제별 리뷰 노트를 Obsidian에 만든다. "이거 정리해줘", "UWorld 오답", "이 문제 틀렸어", "위키에 넣어줘", "이 주제 정리해줘", 문제 해설 스크린샷을 드래그했을 때 사용.
---

# USMLE 주제 리뷰 노트 (`/usmle`)

UWorld 해설 한 장 → **그 주제에 대한 내 모든 자료를 한 노트로 모은다.**
해설 내용 + 내 PDF 자료 + Anki 카드, **전부 출처를 달아서** 원문을 되찾을 수 있게.

- vault: `paths.json`의 `topics` 상위 폴더 (과목 노트가 있는 곳)
- 주제 노트: `<과목>/<주제명>.md` ← 결과물 (과목 폴더 안)
- 과목 MOC: `<과목>.md` ← `## 주제 노트` 아래에 `[[링크]]` 한 줄만 추가
- 복습 큐: `USMLE_Review.md`
- 검색 도구: `mle.py` — 경로는 아래 `$M` 참고. 자료·노트 폴더는 `paths.json`이 정한다

## 절차

### 1. 해설 읽기 → **한국어 해설을 먼저 화면에 낸다**

스크린샷에서 **이미지에 있는 것만** 뽑는다.
- 주제(진단명/개념) — 노트 파일명이 된다
- 정답의 핵심 근거 / 헷갈린 오답의 감별점 (오답이 진짜 학습포인트)
- 표·수치는 마크다운 표로 그대로

흐리거나 잘려서 확신이 안 서면 **추측하지 말고 묻는다.**

**검색·정리를 시작하기 전에, 아래를 먼저 사용자에게 보여준다.** 공부하면서 바로 읽을 것이라 이게 우선이다.

```
## <주제>  ·  QID <번호>  ·  <정답 / 고른 답>

**한 줄**: 이 문제가 실제로 묻는 것

**왜 정답인가** — 2~4줄. 사슬이 있으면 화살표로.

**왜 그걸 골랐나** (오답일 때) — 어디서 갈렸는지 한 줄. 지식 부족인지 문제 오독인지.

**감별** — 헷갈리는 선택지만 한 줄씩. 전부 나열하지 말 것.

**한 문장으로** — 다음에 이 문제를 만나면 떠올릴 것.
```

- **영어 해설을 한국어로 풀어서 설명한다.** 요약이 아니라 해설이다 —
  UWorld가 왜 그렇게 말하는지, 사슬이 어디서 이어지는지를 한국어로 다시 말해준다.
  의학용어는 영어 그대로 (contractility, efferent arteriole, use dependence…).
- **분량은 해설의 밀도에 맞춘다.** 기전 사슬이 길면 길게, 단순 암기면 짧게.
  다만 **노트의 복사본이 되지는 않게** — 여기는 "지금 이해할 것", 노트는 "나중에 찾을 것".
- 표나 그림이 답의 핵심이면 그 표를 여기서 한 번 보여준다. 그래야 화면만 보고 이해된다.
- 해설이 이미지에서 잘렸으면 **그렇다고 말하고**, `qid`로 원문을 찾아 채운 뒤 낸다.
- 이걸 낸 다음에 2~7단계(검색·노트·Anki)를 진행한다.

### 2. 이미 있는지 확인
```bash
find "<CBBSA Study>" -name "*<주제>*.md"                 # ① 같은 이름의 노트가 있나
grep -ril "<주제>" "<CBBSA Study>" --include="*.md"      # ② 다른 노트·MOC 본문에 이미 있나
find ~/Documents/Sync_Vault -name "<주제>.md"             # ③ vault 전체에서 이름이 유일한가
```
같은 주제 노트가 있으면 **새로 만들지 말고 그 노트를 보강**한다 (기존 줄은 안 고침, 새 내용만 추가).

**②를 빼먹지 마라.** 이름만 보면 놓친다 — 아직 독립 노트가 아니고 **과목 MOC나 다른 노트 안에 섹션으로 들어있는** 주제가 있다.
그걸 모르고 새 노트를 만들면 **같은 주제가 두 군데로 갈라진다.** 실제로 그렇게 된 적이 있다
(`Cardiology.md`의 `### Antiarrhythmics`가 있는데 `Class I antiarrhythmics.md`가 따로 생김).

②에 걸리면 둘 중 하나를 고른다:
- 그 섹션이 **본격적인 주제**다 → 섹션을 잘라내 독립 노트로 승격하고, 원래 자리엔 `[[링크]]`만 남긴다
- 그 섹션이 **부분·각주**다 → 새 노트를 만들되 **서로 `[[링크]]`로 잇는다**

**파일명은 vault 453개 노트 전체에서 유일해야 한다** — Obsidian이 경로 없는 `[[파일명]]`으로 링크를 풀기 때문.
애매한 약어는 풀어쓴다 (`PCP`는 Pneumocystis도 되고 phencyclidine도 된다 → `Pneumocystis pneumonia`).

### 3. 검색 — 내 자료와 Anki 둘 다

**스크린샷에 `Question Id`가 보이면 그것부터.** 키워드 검색보다 훨씬 정확하다:
```bash
M="python3 ~/MLE_PREP/mle.py"      # 설치 위치에 맞게
$M qid 161      # 그 문제의 UW PDF 원문 페이지 + 그 문제용으로 만들어진 Anki 카드 직행
```
AnKing 카드에는 `#UWorld::Step::<QID>` 태그가 박혀 있다(Step1 12,086 / Step2 10,957장).
QID가 안 보이면 사용자에게 물어봐도 좋다 — 한 번 물어보는 값이 키워드 검색 여러 번보다 싸다.

**⚠ 같은 QID = 같은 문제가 아니다.** Step1·Step2·COMLEX가 번호를 따로 매기고, 연도판마다 재번호된다.
`qid`가 찍어주는 본문을 **반드시 눈으로 대조**하고, 내용이 다르면 그 히트는 버린다.
노트에 쓸 때도 "QID 일치"가 아니라 **내용이 일치함을 확인했다**는 근거로 쓴다.

**⚠⚠ 인덱스의 UWorld 스크린샷은 타인의 기록이다.** 사용자가 푼 게 아니다.
거기 찍힌 **Correct/Incorrect·선택 답·소요시간은 사용자의 이력이 아니므로 절대 인용하지 마라.**
쓸 수 있는 건 **문제 지문과 해설 본문뿐**이다.
사용자의 정오답은 **오늘 준 스크린샷에서만** 읽는다. 화면에서 잘렸으면 추측하지 말고 물어본다.

**그 다음 로컬은 한 번에 끝낸다. `brief` 하나면 된다** (0.2초):
```bash
$M brief "aortic stenosis murmur"     # 히트 목록 + 상위 히트 원문 + Anki 본문, 전부 한 방에
```
필요할 때만 추가로:
```bash
$M page "Cardiology Slides" 405-412   # 인덱스 원문 그대로. 다른 페이지가 더 필요할 때만
$M tag "Aortic_Stenosis"              # Anki 태그로 훑기
```

- **PDF를 `pdftotext`/Read로 다시 열지 마라.** 17,000여 페이지가 이미 인덱스에 텍스트로 들어있다.
  원문이 더 필요하면 `page` 명령을 쓴다. 파일 경로를 찾을 필요도 없다(파일명 일부만 주면 됨).
- **온라인 검색은 기본적으로 하지 않는다.** 내 자료 + Anki + 네가 아는 USMLE high-yield 수준이면 충분하다.
  PubMed·원저 논문은 찾지 마라 — Step 시험 범위를 넘고 노트만 무거워진다.
  내 자료가 통째로 비어서 확인이 필요할 때만 StatPearls/AAFP 정도를 한 번 본다.
- **내 지식으로 채운 줄은 `^[HY]`로 표시**하고 `## 출처`에 `- \`[HY]\` USMLE high-yield 일반지식 (자료 출처 없음)`을 둔다.
  좌표 있는 줄과 섞이지 않게 하는 게 요점이다.
- **Anki**: `출처태그`에 `#FirstAid::07_Cardiovascular::…` 같은 원본 챕터가 박혀 있다 — 그게 곧 출처다.

**그림이 답의 뼈대면 원본을 오려온다** — `fig`는 두 번 부르는 게 정상이다:
```bash
$M fig FA2026 347                                  # ① 페이지 전체 렌더 → 경로+크기만 찍힌다
                                                   #   Read로 열어보고 좌표(x,y,w,h)를 눈으로 잡는다
$M fig FA2026 347 180,890,1110,740 FA-p347_Lipids  # ② 그 영역만 Assets/Images/에, ![[…]] 줄까지 출력
$M fig ~/Desktop/shot.png 0,80,1200,700 UW_1234_AS # 스크린샷도 같은 방식 (페이지 번호 없음)
```
**①을 건너뛰지 마라.** 렌더된 png를 보지 않고 좌표를 지어내면 엉뚱한 데가 잘린다.

검색어는 해설의 핵심 용어로. 한 번에 안 걸리면 동의어·약어로 2~3번 더 (예: `HCM` / `hypertrophic cardiomyopathy`).

**⚠ 히트 목록만 보고 "그 책엔 없다"고 판단하지 마라.** 히트 목록은 상위 12개(BM25)뿐이라
**책 한 권이 통째로 안 보일 수 있다** — Pathoma에 atherosclerosis가 16쪽 있는데 목록에 하나도 안 떠서
노트에서 통째로 빠진 적이 있다(사용자가 "pathoma도 참고하고 있지?"라고 물어서 발각).
`search`/`brief`가 히트 목록 아래에 **`## 교재별 히트`**를 찍는다. **그걸 반드시 읽고,**
쪽수가 잡히는데 위 목록에 없는 교재는 `page`로 직접 연다.
- **병태생리(pathophysiology) 주제면 Pathoma를 특히 확인한다.** 형태학·합병증·감별을 가장 깔끔하게 정리해준다.
- **아무것도 안 나온 소스는 "없었다"고 노트에 적는다.** 조용히 빼지 않는다.

### 4. 노트 작성 — `<과목>/<주제>.md`
한국어 설명 + 의학용어는 영어. 기존 노트 스타일(`###` + 중첩 불릿, 비교는 표) 유지.

**노트는 "이 문제의 해설"이 아니라 "이 개념의 내 정리"다.** 이게 이 단계의 유일한 원칙이다.
그 문제가 무엇을 물었는지는 1단계 화면 해설에서 이미 다뤘고, 왜 틀렸는지는 6단계 복습 큐로 간다.
여기 남는 건 **1년 뒤 그 문제를 완전히 잊은 채로 읽어도 말이 되는 개념 정리**다.

- **문제 서사를 노트에 옮기지 마라.** "이 문제에서는", "정답은 B", "오답 선택지 D는", "환자가 ~로 왔을 때" —
  전부 문제에 묶인 말이다. 문제를 모르면 못 읽는 줄은 노트에 들어갈 자격이 없다.
- **한 줄 = 한 사실 (point by point).** 문단으로 쓰지 마라. 나중에 훑을 때 눈이 걸려야 하고,
  틀린 줄 하나만 골라 고칠 수 있어야 하고, 줄 단위로 출처가 붙어야 한다.
  기전 사슬은 한 줄 안에서 `→`로 잇고, 하위 근거는 들여쓴 불릿으로.
- **그 문제가 다룬 것보다 넓게 적는다.** 문제는 개념의 한 단면만 물었을 뿐이다.
  자료·Anki에서 나온 인접 사실(진단 기준, 치료, 흔한 함정)은 그 주제 노트에 있어야 할 것이면 같이 넣는다.
  단, **좌표 있는 것만** — 기억으로 넓히지 마라.
- **개념 하나가 여러 문제에서 반복되면 같은 노트가 두꺼워지는 게 정상이다.** 문제별로 노트를 쪼개지 않는다.
- `> 출처:` 헤더 줄에만 QID를 남긴다 — 어디서 왔는지 추적은 되되, 본문은 문제에서 자유롭게.

```markdown
---
tags: [system/cardio, study/path]
created: 2026-07-28
---
# Aortic stenosis

> 출처: UWorld 오답 2026-07-27, QID 1234 · 자료 3 · Anki 5

## 핵심
- Crescendo-decrescendo systolic murmur, carotid로 방사
	- 심해질수록 peak가 늦어짐(late-peaking) → severe 시사 ^[UW]
- **vs HCM**: Valsalva에서 AS는 murmur 감소, HCM은 증가 (preload↓ → outflow obstruction↑) ^[UW][FA]

## 감별
| | Valsalva | Handgrip | 방사 |
| --- | --- | --- | --- |
| AS | ↓ | ↓ | carotid |
| HCM | ↑ | ↓ | 없음 |

![[FA-p301_Murmur-maneuvers.png]] ^[FA-p301]

## 내 자료에서
- Severe AS 기준: valve area <1.0 cm², mean gradient >40 mmHg ^[FA-p.301]

## Anki
- `nid:1471558760048` murmur 모양 = crescendo-decrescendo, soft S2 동반
- 관련 태그: `#AK_Step1_v12::#FirstAid::07_Cardiovascular::…::Aortic_Stenosis`

## 출처
- `[UW]` UWorld 해설 (2026-07-27 오답)
- `[FA-p.301]` FA2026.pdf p.301 (책 p.279) — `materials` 폴더
- `[FA-p301]` FA2026.pdf p.301 그림 — `Assets/Images/FA-p301_Murmur-maneuvers.png`
- `[AK]` Anki `nid:1471558760048` · AnKing Step Deck
- `[HY]` USMLE high-yield 일반지식 (자료 출처 없음)

## 🔗 관련 노트
- [[Cardiology]] · [[Hypertrophic cardiomyopathy]]

## Anki로 돌리기
`nid:1471558760048,1471554803766` — 이 카드들에 `ReviewNeeded` 태그 붙이기
`"tag:#AK_Step1_v12::#FirstAid::07_Cardiovascular::03_Physiology::10_Heart_Murmurs*"` — 주제 전체
```

`## Anki로 돌리기` 섹션은 항상 넣는다.

### 태그는 내가 붙인다 — 노트 저장 직후 실행
```bash
$M mark            # 인용한 카드에 ReviewNeeded 태그 (AnkiConnect)
$M mark wide       # 주제 전체로 넓힐 때
```
- 공부용 쿼리는 언제나 **`tag:ReviewNeeded`** 하나로 고정. 노트가 늘어도 안 바뀐다.
- **태그 외에는 절대 건드리지 않는다.** 카드 내용·스케줄·덱 이동 금지. `addTags`만 쓴다.
- 붙인 뒤 보고에 "N장 태그됨, 덱에서 Rebuild" 한 줄 넣는다.

**⚠ AnkiConnect가 거절되면(Anki 꺼짐) 그걸 보고의 첫 줄에 올린다.** 사용자는 Anki를 항상 켜두지 않아서
**이걸 묻히게 쓰면 태깅이 통째로 누락된 채 잊힌다** — 실제로 "anki 추가했지?"를 되묻게 만든 적이 있다.
- 보고 한 줄의 Anki 자리에 `Anki ⚠ 태그 못 붙임 (Anki 꺼짐)`이라고 **실패를 그대로 적는다.**
  "동기화 완료"처럼 성공한 것처럼 읽히는 문구를 쓰지 마라.
- 이어서 한 줄: **"Anki 켜고 `$M mark` 한 번 돌리면 밀린 것까지 다 붙는다"** —
  `mark`는 매번 노트 전체를 다시 훑으므로 놓친 회차가 자동으로 따라잡힌다. 사용자가 안심할 근거다.
- `$M deck`으로 nid 줄도 같이 준다 (지금 당장 손으로 붙이고 싶을 때).
- **다음 `/usmle` 실행 때 `$M mark`를 먼저 한 번 돌린다.** 지난번에 밀린 게 있으면 여기서 회수된다.

출처 표기 규칙: 본문 각 줄 끝에 `^[약칭]`, 아래 `## 출처`에서 약칭 → **되찾을 수 있는 좌표**(파일명+페이지 / `nid:` / URL)로 푼다. 좌표 없는 출처는 쓰지 않는다.

**PDF 페이지 ≠ 책 인쇄 페이지.** First Aid는 22쪽 차이(PDF p.347 = 책 p.325). 인쇄 번호는
페이지 원문 안에 찍혀 있으니 그대로 읽어서 `FA2026.pdf p.347 (책 p.325)`처럼 둘 다 적는다.

### 5. 연결
- 과목 MOC(`Cardiology.md` 등)의 **`## 주제 노트` 아래**에 `- [[Aortic stenosis]] — <한 줄 설명>`.
  파일 끝에 붙이지 않는다 (`## 🔗 관련 노트` 뒤로 밀리면 안 됨). 과목이 애매하면 묻는다.
- 관련 주제 노트가 이미 있으면 서로 `[[링크]]`.

### 6. 복습 큐
`USMLE_Review.md` 표에 한 줄 (없으면 헤더까지 만든다).

| 날짜 | 과목 | 주제 | 왜 틀렸나 |
| --- | --- | --- | --- |
| 2026-07-27 | Cardiology | [[Aortic stenosis]] | HCM과 Valsalva 반응 반대인 걸 헷갈림 |

"왜 틀렸나"는 지식 부족인지 함정인지 구분해 **한 줄로 구체적으로**.

**맞힌 문제(정리 목적)면 복습 큐에 넣지 않는다.** 오답 신호가 흐려진다. 노트 헤더에만
`(정답 맞힘, 정리용)`으로 남긴다. 오답인지 아닌지 안 밝혔으면 물어본다.

### 7. 인용 검사 — 빼먹지 말 것
```bash
$M check "…/<과목>/<주제>.md"
```
`✗ 출처에 정의 없음`이 뜨면 고친 뒤에 보고한다. 좌표 없는 인용은 이 프로젝트의 존재 이유를 깬다.

### 8. 보고 — 한 줄

```
`<과목>/<주제>.md` 저장 · 인용 N종 ✓ · 그림 N장 · Anki N장 (동기화 완료)
```

**뭔가 실패했으면 그 줄이 맨 앞에 온다.** 성공 보고 뒤에 덧붙이면 묻힌다.
```
⚠ Anki 태그 못 붙임 — Anki가 꺼져 있었다. 켜고 `$M mark` 한 번이면 밀린 것까지 다 붙는다.
`<과목>/<주제>.md` 저장 · 인용 N종 ✓ · 그림 N장 · Anki ✗
```

**노트 내용을 다시 설명하지 마라.** 해설은 1단계에서 이미 했고, 자세한 건 노트에 있다.
여기서 "새로 들어간 것", "핵심 정리" 같은 걸 또 쓰면 같은 말을 세 번 하는 셈이다.

예외로 짧게 덧붙일 것 — **사용자가 몰랐을 만한 것만**:
- 자료에서 아무것도 안 나온 소스가 있었다 (`Pathoma 0건`)
- 기존 노트에 합쳤다 (새로 안 만들고)
- 검증에서 뭔가 걸렸다
- 오답 패턴이 반복되고 있다 (복습 큐에 같은 노트가 2회 이상)

## 규칙

- **좌표 없는 내용은 안 쓴다.** 내 기억으로 보충하고 싶으면 `[확인 필요]` 표시하고 출처 없음을 명시.
- 문제 지문·선택지를 통째로 옮기지 않는다. 학습포인트만 (저작권 + 나중에 노이즈).
- 환자 식별정보 금지.
- 기존 노트의 문장은 고치지 않는다. 틀린 게 보이면 말만 하고 승인받는다.
- **표는 마크다운 표로 옮긴다.** 표는 텍스트가 낫다 — 검색되고, 다른 노트와 비교해 붙일 수 있다.
- **그림은 원본을 오려 넣는다. 다시 그리지 마라.** UW 해설의 도해·FA의 pathway는 `$M fig`로 잘라
  `Assets/Images/`에 넣고 `![[…]]`로 임베드한다. mermaid로 옮겨 그리면 색·배치·억제 화살표가 깎여서
  원본보다 못한 그림이 남는다. **mermaid는 원본 그림이 없을 때만** (텍스트 설명에서 내가 합성한 흐름).
- **이미지도 인용이다.** 임베드 줄 뒤에 `^[UW]` / `^[FA-p347]`를 붙이고 `## 출처`에서 좌표로 푼다.
  파일명이 곧 출처다: `UW_<QID>_<주제>.png`, `FA-p<PDF쪽>_<주제>.png`. **vault 전체에서 유일해야 한다**
  (노트 파일명과 같은 이유 — Obsidian이 경로 없이 이름으로 푼다).
- **UW 스크린샷은 Correct/Incorrect·선택 답·소요시간이 찍힌 영역을 잘라낸다.** 학습 내용만 남긴다.
- 사용자가 준 스크린샷의 **파일 경로가 프롬프트에 없으면 추측하지 말고 묻는다.**
- **노트에 쓸 때 `printf`/`echo`를 쓰지 마라.** 본문에 `%`가 있으면 줄이 잘린다(실제로 겪음).
  Write/Edit 툴로 쓴다. 기존 파일에 덧붙일 때는 파일 끝 개행 여부를 먼저 확인한다.

## 태그 (frontmatter, 최대 2개)

- **`system/*`** (0~1개) — `cardio` `pulm` `renal` `gi` `heme` `endo` `neuro` `psych` `msk` `repro` `derm`
  장기와 무관한 주제(생화학·유전·통계·윤리)는 **안 단다.**
- **`study/*`** (반드시 1개) — `path` `pharm` `physio` `micro` `anat` `biochem` `genetics` `embryo` `behavioral` `biostat`

**폴더는 "어디에 정리했나", 태그는 "무엇을 다루나"** — 둘이 갈리는 게 요점이다.
예: `Coagulase-negative staphylococci`는 `Infection/` 폴더에 있지만 `system/cardio` (심내막염이라 심장 공부할 때 걸려야 한다).

## 자료 폴더

인덱싱 대상은 `paths.json`의 `materials` 배열에 적힌 폴더들. 여러 개 지정할 수 있다.

```bash
$M index   # 바뀐 파일만 다시 읽음
```
새 자료를 넣었는데 검색에 안 잡히면 이걸 먼저 돌린다.

**Sketchy는 그림 위주라 텍스트가 드문드문만 잡힌다** (OCR 미적용). Sketchy 내용은 Anki의
`#Sketchy*` 태그로 보완한다: `mle.py tag SketchyPharm`
